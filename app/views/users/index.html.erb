<table class="table">
  <tr>
  
    <th>Name </th>
    <th>Devices Assigned</th>
    <th>&nbsp;</th>
  </tr>
  
  <tr>
    <% @users.each do |f| %>
    <td><%= link_to f.name, user_path(f) %> </td>
    <td><%= link_to f.devices.size, devices_path(f) %></td>
    <% end %>
  </tr>
</table>
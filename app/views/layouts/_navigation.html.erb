<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">

        <% if logged_in? %>
          <li class="navbar-brand active"><%= link_to_unless_current "#{current_user.first_name} #{current_user.last_name}", user_path(current_user), id: "brandname" %></li>
        <% else %>
          <li class="navbar-brand active">Inventory</li>
        <% end %>
    </div>
    <div class="nav navbar-nav">
      <li class="active"><%= link_to "Users", users_path %></li>
      <li class="active"><%= link_to "Devices", devices_path %></li>
      <% if logged_in? %>
        <li class="active"><%= link_to "Logout", access_logout_path %></li>
      <% end %>
    </div>
  </div>
</nav>
